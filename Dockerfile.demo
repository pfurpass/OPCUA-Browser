FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1
WORKDIR /srv

RUN pip install --no-cache-dir "asyncua==2.0.1" "cryptography>=42.0"
COPY demo/demo_server.py .

RUN useradd --uid 10002 --no-create-home --shell /usr/sbin/nologin demo
USER demo

EXPOSE 4840
CMD ["python", "demo_server.py"]
